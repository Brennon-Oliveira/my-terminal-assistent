import Utils from "../../Utils.js";
const {
    help,
    exec,
    error,
    warning,
    message,
    success,
    getFlags,
    getSettings,
    useTemplate,
    question,
} = Utils;

export default Essentials {
    services = {
        "--help": this.help,
        "-h": this.help,
        fullUpdate: this.pull
    };

    


}